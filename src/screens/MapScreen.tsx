import {View, Text} from 'react-native';
import React from 'react';

const MapScreen = () => {
  return (
    <View style={{display: 'flex', justifyContent: 'center' , alignItems: 'center'}}>
      <Text>Maps will be added later..</Text>
    </View>
  );
};

export default MapScreen;
